[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m waitress --host=0.0.0.0 --port=$PORT app_production:app"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
